import type { Region } from '@redocly/openapi-core/lib/config/types';
export interface RegionalToken {
    region: Region;
    token: string;
}
export interface RegionalTokenWithValidity extends RegionalToken {
    valid: boolean;
}
